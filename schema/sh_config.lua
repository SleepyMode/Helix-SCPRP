
-- Currency configuration
ix.currency.symbol = "$"
ix.currency.singular = "USD"
ix.currency.singular = "USD"

-- Make it so that characters are considered
-- recognised in the scoreboard by default.
-- This will make players be able to see
-- the names and avatars(models) of other
-- players in the scoreboard, but doesn't
-- mean that the players actually recognise
-- the characters, so that when interacting
-- in character it would still show others
-- as unrecognised.
-- NOTE: This is no longer the case.
-- ix.config.SetDefault("scoreboardRecognition", true)